<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPimaryDark</item>
        <item name="colorAccent">@color/colorPimaryDark</item>
        <item name="android:textViewStyle">@style/TextStyle</item>
        <item name="colorButtonNormal">@color/colorPrimary</item>
        <item name="android:datePickerDialogTheme">@style/AppDatePickerDialogTheme</item>
    </style>

    <style name="AppDatePickerDialogTheme" parent="android:Theme.Material.Dialog">
        <item name="android:datePickerStyle">@style/AppDatePickerStyle</item>
    </style>

    <style name="AppDatePickerStyle" parent="android:Widget.Material.DatePicker">
        <item name="android:datePickerMode">spinner</item>
    </style>

    <style name="TextStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">sans-serif-light</item>
        <!--<item name="android:textStyle">bold</item>-->
    </style>

</resources>
